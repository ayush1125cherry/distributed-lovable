spring:
  application:
    name: config-service

  cloud:
    config.server.git:
          uri: https://github.com/ayush1125cherry/lovable-config-server
          username: ayush1125cherry
          password: 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
          default-label: master

server:
  port: 8888


management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/ayush1125cherry/lovable-config-server
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

